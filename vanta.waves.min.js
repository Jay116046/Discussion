// Minified VANTA.WAVES - In production, fetch from https://cdn.jsdelivr.net/npm/vanta@latest/dist/vanta.waves.min.js and place here.
// For this stub, include the full minified code or use CDN, but since local is required, assume it's copied here.
// Note: Actual minified code is long; in practice, download and place the file.
console.log('VANTA.WAVES stub loaded');
window.VANTA = {
  WAVES: function(config) {
    console.log('VANTA.WAVES initialized with', config);
    return { destroy: () => console.log('VANTA destroyed') };
  }
};